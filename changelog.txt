2025-12-04
 - Fixed some grammar issue and a small bug when user clicks the very edges of the block
   grid SVG.
2025-07-15 v4.1.1 (minor change)
 - Added info in "C800DataFormatExplained.html" to explain on how to convert XY position
   to layer 2.
 - Fixed negative coordinates in the "JS_C800Calculator.html" when hovering the mouse
   over the left and/or top margin of the SVG.
2025-07-15 v4.1.0
 - C800 calculator updated so that instead of disabling the other inputs if the user
   enters an out of range value, it instead changes background of the the input the user
   entered red.
 - C800DataFormatExplained.html changes
 -- Added images I forgot to include in v4.
 -- Added info about that layer 2 is in the same way as layer 1 on the arrangement of
    blocks that I forgot to include in v4.
 -- Explained better on what's flagged as out of bounds in the ASM codes, and that the
    JavaScript HTML calculator always assumes the full width as the level bounds.
 - Corrected the info about (BlocksPerScrnCol * floor(XPos/16)) on the image.
 - Fixed a typo on the image showing a data gap between layer 1 and layer 2.
2025-07-13 v4
 - C800 calculator changes:
 -- No longer using innerHTML and instead use textContent and createElement.
 -- Statistics now graphically displays the $C800-$FFFF usage.
 -- Included an interactive tilemap display.
 -- Hex text inputs now supports up/down inputs to increase/decrease the value.
 -- You can now enter hex or dec for XY positions
 -- Unused data of $C800 are now explained to the user.
 - Dark mode added on both HTMLs
 - added asar functions that converts index to XY position
2020-07-19 v3:
 - some HTML changes

2019-06-02 v2:
 - Adopted to newer LM version v3 that have custom level dimension routines.

201607-01 v1:
 - First version